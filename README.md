
  # Build React Web UI

  This is a code bundle for Build React Web UI. The original project is available at https://www.figma.com/design/Ca30W5qKZuDBCrICpu9pwS/Build-React-Web-UI.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  